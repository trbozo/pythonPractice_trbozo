# // double notOrtalamasi = 75.0;
# // String durum = notOrtalamasi >= 50.0 ? "Geçti" : "Kaldı";
# // System.out.println(durum);
not_ortalamasi = 75.0
durum = "Geçti" if not_ortalamasi >= 50.0 else "Kaldı"
print(durum)
